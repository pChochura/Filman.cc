--- app/src/main/java/com/example/filman/ui/player/PlayerScreen.kt
+++ app/src/main/java/com/example/filman/ui/player/PlayerScreen.kt
@@ -25,2 +25,3 @@
 import androidx.compose.foundation.focusable
+import androidx.compose.ui.focus.onFocusChanged
 import androidx.compose.ui.focus.focusRequester
@@ -67,2 +68,5 @@
 
+    var lastInteractionTime by remember { mutableLongStateOf(System.currentTimeMillis()) }
+    val playPauseFocusRequester = remember { FocusRequester() }
+
     var currentPos by remember { mutableStateOf(0L) }
@@ -99,4 +103,4 @@
     // Auto-hide overlay
-    LaunchedEffect(isOverlayVisible, isSettingsVisible, isPlaying) {
+    LaunchedEffect(isOverlayVisible, isSettingsVisible, isPlaying, lastInteractionTime) {
         if (isOverlayVisible && !isSettingsVisible && isPlaying) {
-            delay(3000)
+            delay(5000)
             isOverlayVisible = false
@@ -213,20 +217,45 @@
             .onKeyEvent { event ->
                 if (event.type == KeyEventType.KeyDown) {
+                    lastInteractionTime = System.currentTimeMillis()
                     val key = event.key
                     when (key) {
                         Key.DirectionCenter, Key.Enter, Key.NumPadEnter -> {
                             if (!isOverlayVisible) {
                                 isOverlayVisible = true
+                                scope.launch {
+                                    delay(100)
+                                    try { playPauseFocusRequester.requestFocus() } catch (e: Exception) {}
+                                }
                                 true
                             } else false
                         }
-                        Key.DirectionUp, Key.DirectionDown, Key.DirectionLeft, Key.DirectionRight -> {
+                        Key.DirectionLeft -> {
                             if (!isOverlayVisible) {
+                                exoPlayer?.let { player ->
+                                    player.seekTo((player.currentPosition - 15000).coerceAtLeast(0))
+                                }
+                                true
+                            } else {
                                 isOverlayVisible = true
+                                false
+                            }
+                        }
+                        Key.DirectionRight -> {
+                            if (!isOverlayVisible) {
+                                exoPlayer?.let { player ->
+                                    player.seekTo((player.currentPosition + 15000).coerceAtMost(player.duration))
+                                }
+                                true
+                            } else {
+                                isOverlayVisible = true
+                                false
                             }
-                            false
                         }
-                        Key.Back, Key.Escape -> {
+                        Key.DirectionUp, Key.DirectionDown -> {
+                            if (!isOverlayVisible) {
+                                isOverlayVisible = true
+                                scope.launch {
+                                    delay(100)
+                                    try { playPauseFocusRequester.requestFocus() } catch (e: Exception) {}
+                                }
+                                true
+                            } else false
+                        }
+                        Key.Back, Key.Escape -> {
